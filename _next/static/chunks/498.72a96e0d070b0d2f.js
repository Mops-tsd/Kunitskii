"use strict";(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[498],{2498:(e,n,o)=>{o.r(n),o.d(n,{HeroCanvas:()=>S});var t=o(5155),r=o(2115),a=o(258),l=o(5339),i=o(9037);let u=201;function s(e){let n,o=arguments.length>1&&void 0!==arguments[1]?arguments[1]:0x1348ad6,t=(n=o>>>0,()=>{let e=n=n+0x6d2b79f5>>>0;return e=Math.imul(e^e>>>15,1|e),(((e^=e+Math.imul(e^e>>>7,61|e))^e>>>14)>>>0)/0x100000000}),r=[];for(let n of function(){let e=[];for(let n=-5;n<5;n+=1)for(let o=-5;o<5;o+=1){let t=(n+.5)*37;e.push({x:t+16*Math.sign(t),z:(o+.5)*37,i:n,j:o})}return e.sort((e,n)=>Math.hypot(e.x,e.z)-Math.hypot(n.x,n.z)),e}()){if(r.length>=e)break;let o=Math.hypot(n.x,n.z),a=1-Math.min(o/u,1),l=Math.round(4+19*Math.pow(a,1.7)+4*t()),i=(n.i+n.j)%2==0,s=l>16?1:l>9?2:2+Math.round(t()),f=(26-4*(s-1))/s,v=13+4*t(),d=(t()-.5)*(26-v);for(let c=0;c<s&&r.length<e;c+=1){let e=-13+f/2+c*(f+4),s=f*(.86+.14*t()),m=Math.max(3,l+Math.round((t()-.5)*3));r.push({x:n.x+(i?e:d),z:n.z+(i?d:e),width:i?s:v,depth:i?v:s,storeys:m,height:3.1*m,delay:.55*Math.min(o/u,1)+.18*t(),pileDepth:5+8*a+3*t(),seed:.6180339887*r.length%1})}}return r}var f=o(1243),v=o(9567);function d(e){let{count:n}=e,o=(0,r.useRef)(null),a=(0,r.useMemo)(()=>s(n),[n]),u=(0,r.useMemo)(()=>{let e=new l.iNn(1,1,1);e.translate(0,.5,0);let n=new Float32Array(3*a.length),o=new Float32Array(a.length),t=new Float32Array(a.length);return a.forEach((e,r)=>{n[3*r+0]=e.width,n[3*r+1]=e.depth,n[3*r+2]=e.height,o[r]=e.delay,t[r]=e.seed}),e.setAttribute("aDims",new l.uWO(n,3)),e.setAttribute("aDelay",new l.uWO(o,1)),e.setAttribute("aSeed",new l.uWO(t,1)),e},[a]),d=(0,r.useMemo)(()=>new l.BKk({vertexShader:"\n  attribute vec3 aDims;      // ширина, глубина, высота дома в метрах\n  attribute float aDelay;    // сдвиг начала роста, 0..1\n  attribute float aSeed;     // персональное зерно для окон\n\n  uniform float uBuild;      // прогресс волны работ по всей площадке\n  uniform float uLights;     // насколько город обжит — общий множитель окон\n  uniform float uStorey;     // высота этажа в метрах\n\n  varying vec2 vUv;\n  varying vec2 vGrid;        // координаты в метрах — по ним рисуется фасад\n  varying vec3 vNormalW;\n  varying float vFill;       // насколько заполнен конкретный дом\n  varying float vLit;        // насколько горят окна этого дома\n  varying float vSeed;\n  varying float vDepth;\n  varying float vTop;        // высота дома на этом кадре, в метрах\n\n  void main() {\n    /*\n     * Личное время дома. Волна работ расходится от центра наружу:\n     * каждый дом проходит свой путь целиком — каркас, следом бетон, —\n     * а не ждёт, пока весь город достоит скелет.\n     */\n    float local = uBuild * 1.6 - aDelay * 0.85;\n\n    float grow = smoothstep(0.0, 0.30, local);\n    float fill = smoothstep(0.34, 0.66, local);\n\n    /*\n     * Дом растёт этажами, а не тянется вверх плавно: очередная захватка\n     * набирается быстро, потом пауза до следующей. Непрерывный рост\n     * читается как резиновый — именно эта деталь сильнее всего выдавала,\n     * что смотришь на анимацию, а не на стройку.\n     */\n    float floors = aDims.z / uStorey * grow;\n    float shaped = floor(floors) + smoothstep(0.0, 0.6, fract(floors));\n    float h = shaped * uStorey;\n\n    vec3 p = position;\n    p.x *= aDims.x;\n    p.z *= aDims.y;\n    p.y *= h;\n\n    // Фасад размечается в метрах, иначе окна растягивались бы вместе\n    // с домом и на высотке были бы вчетверо крупнее.\n    if (abs(normal.y) > 0.5) {\n      vGrid = vec2(uv.x * aDims.x, uv.y * aDims.y);\n    } else if (abs(normal.x) > 0.5) {\n      vGrid = vec2(uv.x * aDims.y, uv.y * h);\n    } else {\n      vGrid = vec2(uv.x * aDims.x, uv.y * h);\n    }\n\n    vUv = uv;\n    vFill = fill;\n    // Свет появляется только в достроенном доме и только когда\n    // на площадку опускается вечер.\n    vLit = fill * uLights;\n    vSeed = aSeed;\n    vTop = h;\n    vNormalW = normalize(mat3(instanceMatrix) * normal);\n\n    vec4 world = instanceMatrix * vec4(p, 1.0);\n    vec4 viewPos = modelViewMatrix * world;\n    vDepth = -viewPos.z;\n\n    gl_Position = projectionMatrix * viewPos;\n  }\n",fragmentShader:"\n  precision highp float;\n\n  uniform vec3 uOutline;   // цвет линии чертежа\n  uniform vec3 uConcrete;  // цвет стены\n  uniform vec3 uRoof;      // цвет кровли\n  uniform vec3 uWindow;    // цвет света в окнах\n  uniform vec3 uFogColor;\n  uniform float uTime;\n  uniform float uStorey;\n  uniform float uFogNear;\n  uniform float uFogFar;\n  /*\n   * Прогресс стройки и вечера — теми же значениями, что и в вершинном\n   * шейдере. Нужны здесь как переключатели: по ним пропускаются целые\n   * куски расчёта, когда они заведомо ни на что не влияют.\n   *\n   * Ветвление по uniform для видеокарты бесплатно: значение одно на весь\n   * кадр, поэтому все пиксели идут одной веткой и вторая действительно\n   * не считается. Так делать можно далеко не всегда — на ветвлении по\n   * данным пикселя вышло бы наоборот.\n   */\n  uniform float uBuild;\n  uniform float uLights;\n\n  varying vec2 vUv;\n  varying vec2 vGrid;\n  varying vec3 vNormalW;\n  varying float vFill;\n  varying float vLit;\n  varying float vSeed;\n  varying float vDepth;\n  varying float vTop;\n\n  /*\n   * Хеш без sin(). Привычный fract(sin(dot(p, ...)) * 43758.5) на больших\n   * аргументах разваливается: во float32 у синуса не остаётся точности,\n   * и вместо одного значения на окно получалась рябь внутри каждого окна.\n   */\n  float hash(vec2 p) {\n    vec3 q = fract(vec3(p.xyx) * 0.1031);\n    q += dot(q, q.yzx + 33.33);\n    return fract((q.x + q.y) * q.z);\n  }\n\n  /** Линия постоянной экранной толщины вокруг значения 0 или 1 у coord. */\n  float ruledLine(float coord, float width) {\n    float d = min(fract(coord), 1.0 - fract(coord));\n    return 1.0 - smoothstep(0.0, width * fwidth(coord), d);\n  }\n\n  void main() {\n    vec3 n = normalize(vNormalW);\n    float roofness = step(0.5, abs(n.y));\n\n    // --- контур грани, толщиной ровно в пиксель на любом расстоянии\n    vec2 fw = fwidth(vUv);\n    vec2 edgeDist = min(vUv, 1.0 - vUv) / max(fw, vec2(1e-5));\n    float edge = 1.0 - smoothstep(0.6, 2.2, min(edgeDist.x, edgeDist.y));\n\n    // --- разметка фасада в этажах\n    float floorCoord = vGrid.y / uStorey;\n    float floorId = floor(floorCoord);\n    float inFloor = fract(floorCoord);\n\n    // Шаг оконной оси. 3.2 м — обычная ширина комнаты в панельном доме.\n    float bayCoord = vGrid.x / 3.2;\n    float bayId = floor(bayCoord);\n    float inBay = fract(bayCoord);\n\n    /*\n     * Каркас: перекрытия и стойки. Пока объём не налит, дом выглядит\n     * ровно так, как выглядит недостроенный — стопкой плит на колоннах.\n     * Раньше на этой фазе была видна только коробка, и стройка читалась\n     * как проволочная модель.\n     */\n    float slab = ruledLine(floorCoord, 1.6) * (1.0 - roofness);\n    float skeleton = slab;\n    // Стойки нужны только пока идут работы: на готовом доме их не видно,\n    // а лишний fwidth на каждый пиксель фасада — заметная часть кадра.\n    if (uBuild < 0.999) {\n      float column = ruledLine(bayCoord * 0.5, 1.4) * (1.0 - roofness);\n      skeleton = max(slab, column * 0.55);\n    }\n\n    // --- окно внутри этажа: снизу глухой пояс перекрытия, сверху перемычка\n    float pane =\n      smoothstep(0.13, 0.19, inBay) * (1.0 - smoothstep(0.81, 0.87, inBay)) *\n      smoothstep(0.30, 0.36, inFloor) * (1.0 - smoothstep(0.86, 0.92, inFloor));\n    pane *= 1.0 - roofness;\n    // Недостроенный верхний этаж окон ещё не имеет — иначе на срезе\n    // растущего дома висел бы ряд половинок.\n    pane *= step((floorId + 1.0) * uStorey, vTop + 0.01);\n\n    float lit = 0.0;\n    if (uLights > 0.001) {\n      /*\n       * Свет горит квартирами, а не отдельными окнами: одно зерно на пару\n       * соседних осей. Случайное посветление каждого окна по отдельности\n       * давало рябь, из-за которой фасад читался как шум.\n       */\n      float roll = hash(vec2(floor(bayId * 0.5), floorId) + vSeed * 11.0);\n      // Нижние этажи обжиты плотнее — там входные группы и встроенные помещения.\n      float lowBoost = 1.0 + 0.25 * (1.0 - smoothstep(0.0, 7.0, floorId));\n      lit = step(0.68, roll * lowBoost);\n      // Часть окон медленно \xabдышит\xbb — город выглядит живым, а не отрендеренным.\n      lit *= 0.7 + 0.3 * sin(uTime * 0.6 + roll * 43.0);\n      // Внутри квартиры окна светят чуть по-разному.\n      lit *= 0.75 + 0.35 * hash(vec2(bayId, floorId) + vSeed);\n    }\n\n    // --- освещение\n    vec3 lightDir = normalize(vec3(-0.45, 0.72, 0.52));\n    float lambert = 0.16 + 0.84 * max(dot(n, lightDir), 0.0);\n    // Небо светит сверху: горизонтальные грани всегда светлее вертикальных.\n    float sky = 0.45 + 0.55 * (n.y * 0.5 + 0.5);\n    // У земли между корпусами темнее — свет туда не доходит.\n    float ao = mix(0.45, 1.0, smoothstep(0.0, 14.0, vGrid.y));\n\n    vec3 surface = mix(uConcrete, uRoof, roofness);\n    // Тёмное стекло: непогашенное окно должно быть темнее стены, а не\n    // просто её повторять — иначе фасад выглядит глухой панелью.\n    surface = mix(surface, surface * 0.42, pane);\n    float shade = mix(lambert * 0.75 + sky * 0.45, sky * 0.8, roofness);\n\n    vec3 color = surface * shade * mix(ao, 1.0, roofness) * vFill;\n\n    /*\n     * Каркас гасится с расстоянием отдельно от тумана: дом рядом должен\n     * читаться линиями, а полсотни таких же за ним — уже нет, иначе\n     * экран забивается сеткой и в ней тонет всё остальное.\n     */\n    float nearFrame = 0.25 + 0.75 * (1.0 - smoothstep(35.0, 150.0, vDepth));\n\n    // Перекрытия видны и на готовом доме — межэтажным поясом.\n    color += uOutline * slab * (0.14 + 0.28 * (1.0 - vFill)) * nearFrame;\n    color += uOutline * skeleton * (1.0 - vFill) * 0.30 * nearFrame;\n    color += uWindow * pane * lit * vLit * 1.15;\n    // Контур виден с самого начала и слегка гаснет, когда появился объём.\n    color += uOutline * edge * (0.62 - 0.32 * vFill);\n\n    // Туман прячет край сцены — дальние дома тают, а не обрываются.\n    // Он работает цветом, а не прозрачностью: полупрозрачный дом\n    // показывал сквозь себя окна тех, что стоят за ним, и весь фасад\n    // рассыпался в рябь.\n    float fog = smoothstep(uFogNear, uFogFar, vDepth);\n    color = mix(color, uFogColor, fog);\n\n    /*\n     * Прозрачность зависит только от того, налит ли объём. Пока дом —\n     * каркас, видны одни линии и сквозь них смотрится вся стройка;\n     * как только появился бетон, грань становится глухой.\n     */\n    float ink = max(edge, max(skeleton, slab));\n    /*\n     * Грань становится глухой, едва появился бетон, — раньше, чем стена\n     * доберёт свой цвет. Пока alpha шла вровень с заливкой, сквозь\n     * недокрашенный фасад просвечивали окна противоположной стены.\n     */\n    float alpha = clamp(max(smoothstep(0.0, 0.3, vFill), ink), 0.0, 1.0);\n    if (alpha < 0.01) discard;\n\n    gl_FragColor = vec4(color, alpha);\n  }\n",transparent:!0,depthWrite:!0,uniforms:{uBuild:{value:0},uLights:{value:0},uTime:{value:0},uStorey:{value:3.1},uOutline:{value:new l.Q1f(v.A.outline)},uConcrete:{value:new l.Q1f(v.A.concrete)},uRoof:{value:new l.Q1f(v.A.roof)},uWindow:{value:new l.Q1f(v.A.window)},uFogColor:{value:new l.Q1f(v.A.void)},uFogNear:{value:90},uFogFar:{value:340}}}),[]);return(0,r.useLayoutEffect)(()=>{let e=o.current;if(!e)return;let n=new l.kn4;a.forEach((o,t)=>{n.makeTranslation(o.x,0,o.z),e.setMatrixAt(t,n)}),e.instanceMatrix.needsUpdate=!0,e.frustumCulled=!1},[a]),(0,i.D)((e,n)=>{let o=d.uniforms;o.uTime.value=(o.uTime.value+n)%600,o.uBuild.value=(0,f.Ln)(f.Me.progress,f.A9.build[0],f.A9.build[1]),o.uLights.value=(0,f.Ln)(f.Me.progress,f.A9.lights[0],f.A9.lights[1]);let t=o.uBuild.value>.985;d.transparent===t&&(d.transparent=!t,d.needsUpdate=!0)}),(0,t.jsx)("instancedMesh",{ref:o,args:[u,d,a.length],renderOrder:2})}function c(e){let{count:n}=e,o=(0,r.useRef)(null),a=(0,r.useMemo)(()=>s(n),[n]),u=(0,r.useMemo)(()=>a.flatMap(e=>{let n=.32*e.width,o=.32*e.depth;return[[-n,-o],[n,-o],[n,o],[-n,o]].map((n,o)=>{let[t,r]=n;return{x:e.x+t,z:e.z+r,depth:e.pileDepth,delay:Math.min(e.delay+.03*o,1)}})}),[a]),d=(0,r.useMemo)(()=>{let e=new l.Ho_(.17,.1,1,6,1,!0);e.translate(0,.5,0);let n=new Float32Array(u.length),o=new Float32Array(u.length);return u.forEach((e,t)=>{n[t]=e.depth,o[t]=e.delay}),e.setAttribute("aDepth",new l.uWO(n,1)),e.setAttribute("aDelay",new l.uWO(o,1)),e},[u]),c=(0,r.useMemo)(()=>new l.BKk({vertexShader:"\n  attribute float aDepth;\n  attribute float aDelay;\n\n  uniform float uProgress;\n\n  varying float vDrive;   // насколько эта свая уже забита\n  varying float vWorldY;  // высота точки над площадкой, в метрах\n\n  void main() {\n    float drive = clamp((uProgress - aDelay * 0.55) / 0.45, 0.0, 1.0);\n\n    /*\n     * Погружение идёт ударами: копёр бьёт, свая уходит на захватку,\n     * пауза. Плавное скольжение вниз читалось как \xabдеталь въезжает\n     * в сцену\xbb, а не как работа.\n     */\n    float blows = 7.0;\n    float step = drive * blows;\n    drive = (floor(step) + smoothstep(0.0, 0.45, fract(step))) / blows;\n\n    // Свая целиком стоит над площадкой и опускается, пока сверху\n    // не останется торчать только оголовок.\n    float head = 0.35;\n    vec3 p = position;\n    p.y = p.y * aDepth - (aDepth - head) * drive;\n\n    vDrive = drive;\n\n    vec4 world = instanceMatrix * vec4(p, 1.0);\n    vWorldY = world.y;\n    gl_Position = projectionMatrix * modelViewMatrix * world;\n  }\n",fragmentShader:"\n  precision mediump float;\n\n  uniform vec3 uColor;\n  uniform float uDepthFade;   // глубина, на которой свая полностью гаснет\n\n  varying float vDrive;\n  varying float vWorldY;\n\n  void main() {\n    // Ниже нуля — грунт: там свая видна вполсилы и гаснет с глубиной.\n    float underground = 1.0 - smoothstep(0.0, -uDepthFade, vWorldY);\n    float alpha = mix(0.22 * underground, 0.7, step(0.0, vWorldY));\n\n    // Пока свая не тронулась, её ещё не привезли.\n    alpha *= smoothstep(0.0, 0.05, vDrive);\n\n    if (alpha < 0.01) discard;\n    gl_FragColor = vec4(uColor, alpha);\n  }\n",transparent:!0,depthWrite:!1,side:l.$EB,blending:l.EZo,uniforms:{uProgress:{value:0},uDepthFade:{value:14},uColor:{value:new l.Q1f(v.A.pile)}}}),[]);return(0,r.useLayoutEffect)(()=>{let e=o.current;if(!e)return;let n=new l.kn4;u.forEach((o,t)=>{n.makeTranslation(o.x,0,o.z),e.setMatrixAt(t,n)}),e.instanceMatrix.needsUpdate=!0,e.frustumCulled=!1},[u]),(0,i.D)(()=>{c.uniforms.uProgress.value=(0,f.Ln)(f.Me.progress,f.A9.piles[0],f.A9.piles[1])}),(0,t.jsx)("instancedMesh",{ref:o,args:[d,c,u.length],renderOrder:1})}function m(){let e=(0,r.useMemo)(()=>new l.BKk({vertexShader:"\n  varying vec2 vWorld;\n  void main() {\n    vec4 world = modelMatrix * vec4(position, 1.0);\n    vWorld = world.xz;\n    gl_Position = projectionMatrix * viewMatrix * world;\n  }\n",fragmentShader:"\n  precision mediump float;\n\n  uniform float uReveal;    // радиус проявления сетки\n  uniform float uTime;\n  uniform float uRetire;    // 0 — чертёж на месте, 1 — сетка убрана\n  uniform vec3 uMinor;\n  uniform vec3 uMajor;\n\n  varying vec2 vWorld;\n\n  // Линия постоянной толщины независимо от угла обзора.\n  float gridLine(vec2 coord, float step) {\n    vec2 g = abs(fract(coord / step - 0.5) - 0.5) / fwidth(coord / step);\n    return 1.0 - min(min(g.x, g.y), 1.0);\n  }\n\n  void main() {\n    float minor = gridLine(vWorld, 2.0);\n    float major = gridLine(vWorld, 10.0);\n\n    float dist = length(vWorld);\n\n    // Сетка расходится от центра кольцом, а не включается разом.\n    float reveal = 1.0 - smoothstep(uReveal - 18.0, uReveal, dist);\n    // Бегущий световой обод по фронту проявления.\n    float ring = exp(-pow((dist - uReveal) / 9.0, 2.0)) * 0.9;\n\n    float fade = 1.0 - smoothstep(40.0, 230.0, dist);\n\n    /*\n     * Мелкая сетка уходит первой. С высоты отлёта шаг в два метра\n     * складывается в муар, и район выглядел листом миллиметровки:\n     * к финалу от чертежа остаются только крупные оси, и то вполсилы.\n     */\n    vec3 color = uMinor * minor * 0.5 * (1.0 - uRetire) + uMajor * major;\n    float alpha =\n      (minor * 0.28 * (1.0 - uRetire) + major * 0.6 * (1.0 - uRetire * 0.75)) *\n        reveal * fade +\n      ring * fade * 0.5;\n\n    if (alpha < 0.004) discard;\n    gl_FragColor = vec4(color + uMajor * ring, alpha);\n  }\n",transparent:!0,depthWrite:!1,uniforms:{uReveal:{value:0},uTime:{value:0},uRetire:{value:0},uMinor:{value:new l.Q1f(v.A.gridMinor)},uMajor:{value:new l.Q1f(v.A.gridMajor)}}}),[]),n=(0,r.useRef)(null);return(0,i.D)((o,t)=>{e.uniforms.uTime.value+=t;let r=(0,f.Ln)(f.Me.progress,f.A9.gridIn[0],f.A9.gridIn[1]);e.uniforms.uReveal.value=250*r;let a=(0,f.Ln)(f.Me.progress,f.A9.lights[0],f.A9.lights[1]);e.uniforms.uRetire.value=a;let l=n.current;l&&(l.visible=a<.995)}),(0,t.jsx)("mesh",{ref:n,rotation:[-Math.PI/2,0,0],position:[0,.03,0],material:e,renderOrder:0,frustumCulled:!1,children:(0,t.jsx)("planeGeometry",{args:[560,560]})})}function p(){let e=(0,r.useMemo)(()=>new l.BKk({vertexShader:"\n  varying vec2 vWorld;\n  void main() {\n    vec4 world = modelMatrix * vec4(position, 1.0);\n    vWorld = world.xz;\n    gl_Position = projectionMatrix * viewMatrix * world;\n  }\n",fragmentShader:"\n  precision mediump float;\n\n  uniform float uReveal;    // радиус залитой плиты, в метрах\n  uniform float uPitch;     // шаг уличной сетки\n  uniform float uStreet;    // ширина проезда\n  uniform float uAvenue;    // насколько осевой проспект шире рядового проезда\n  uniform vec3 uSlab;\n  uniform vec3 uStreetColor;\n  uniform vec3 uFogColor;\n  uniform vec3 uEdge;\n\n  varying vec2 vWorld;\n\n  float hash(vec2 p) {\n    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);\n  }\n\n  void main() {\n    float dist = length(vWorld);\n\n    // Плита разливается кольцом от центра; за фронтом ничего нет.\n    float poured = 1.0 - smoothstep(uReveal - 10.0, uReveal, dist);\n    if (poured < 0.02) discard;\n\n    /*\n     * Расстояние до ближайшей оси улицы. Кварталы сдвинуты на полшага,\n     * поэтому оси сетки приходятся ровно на проезды.\n     *\n     * По оси X застройка дополнительно раздвинута на ширину проспекта\n     * (см. cityLayout) — здесь этот сдвиг снимается, иначе разметка на\n     * плите не совпала бы с домами, которые на ней стоят.\n     */\n    float ax = abs(vWorld.x);\n    float lattice = max(ax - uAvenue, 0.0);\n    vec2 toAxis = vec2(\n      abs(fract(lattice / uPitch + 0.5) - 0.5) * uPitch,\n      abs(fract(vWorld.y / uPitch + 0.5) - 0.5) * uPitch\n    );\n    // Внутри проспекта покрытие сплошное, разбивать его на кварталы нечем.\n    float onAvenue = 1.0 - smoothstep(uAvenue - 1.0, uAvenue + uStreet * 0.5, ax);\n    float nearest = min(toAxis.x, toAxis.y);\n    float onStreet = max(\n      1.0 - smoothstep(uStreet * 0.34, uStreet * 0.5, nearest),\n      onAvenue\n    );\n\n    // Крупное пятно неровности: снег ложится неравномерно, ровная\n    // заливка выглядит как пластик.\n    float grain = hash(floor(vWorld * 0.45)) * 0.16;\n\n    vec3 color = mix(uSlab, uStreetColor, onStreet) * (0.88 + grain);\n\n    // Осевая разметка по центру проезда и по оси проспекта.\n    float axis = max(\n      1.0 - smoothstep(0.0, 0.5, nearest),\n      1.0 - smoothstep(0.0, 0.5, ax)\n    );\n    color += uEdge * axis * 0.1;\n\n    // Светящийся фронт заливки.\n    float front = exp(-pow((dist - uReveal) / 6.0, 2.0));\n    color += uEdge * front * 0.55;\n\n    // Дальний край уводим в туман, чтобы плита не обрывалась линией.\n    color = mix(color, uFogColor, smoothstep(110.0, 330.0, dist));\n\n    gl_FragColor = vec4(color, 1.0);\n  }\n",transparent:!1,depthWrite:!0,uniforms:{uReveal:{value:0},uPitch:{value:37},uStreet:{value:11},uAvenue:{value:16},uSlab:{value:new l.Q1f(v.A.slab)},uStreetColor:{value:new l.Q1f(v.A.street)},uFogColor:{value:new l.Q1f(v.A.void)},uEdge:{value:new l.Q1f(v.A.gridMajor)}}}),[]);return(0,i.D)(()=>{let n=(0,f.Ln)(f.Me.progress,f.A9.slab[0],f.A9.slab[1]);e.uniforms.uReveal.value=430*n}),(0,t.jsx)("mesh",{rotation:[-Math.PI/2,0,0],position:[0,0,0],material:e,renderOrder:0,frustumCulled:!1,children:(0,t.jsx)("planeGeometry",{args:[720,720]})})}function h(){let e=(0,r.useMemo)(()=>new l.BKk({vertexShader:"\n  varying vec3 vDir;\n  void main() {\n    vDir = position;\n    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n  }\n",fragmentShader:"\n  precision mediump float;\n\n  uniform vec3 uVoid;\n  uniform vec3 uHorizon;\n\n  varying vec3 vDir;\n\n  void main() {\n    float height = normalize(vDir).y;\n    // Узкая полоса у горизонта, симметрично вверх и вниз.\n    float glow = exp(-abs(height) * 2.6);\n    gl_FragColor = vec4(mix(uVoid, uHorizon, glow), 1.0);\n  }\n",side:l.hsX,depthWrite:!1,depthTest:!0,fog:!1,uniforms:{uVoid:{value:new l.Q1f(v.A.void)},uHorizon:{value:new l.Q1f(v.A.horizon)}}}),[]),n=(0,r.useRef)(null);return(0,i.D)(e=>{var o;let{camera:t}=e;null==(o=n.current)||o.position.copy(t.position)}),(0,t.jsx)("mesh",{ref:n,material:e,renderOrder:900,frustumCulled:!1,children:(0,t.jsx)("sphereGeometry",{args:[600,24,16]})})}function g(e){let{count:n}=e,o=(0,r.useMemo)(()=>(function(e){let n=s(e).map((e,n)=>({b:e,i:n,radius:Math.hypot(e.x,e.z)})).filter(e=>e.radius>22&&e.radius<95).sort((e,n)=>n.b.height-e.b.height),o=[];for(let e of n){if(o.length>=5)break;o.some(n=>34>Math.hypot(n.x-e.b.x,n.z-e.b.z))||o.push({x:e.b.x+.5*e.b.width+5,z:e.b.z,height:e.b.height+7,angle:1.7*e.i,slew:.05+e.i%5*.012})}return o})(n),[n]),a=(0,r.useRef)([]),u=(0,r.useMemo)(()=>o.map(e=>{let n=new l.LoY;return n.setAttribute("position",new l.THS(function(e){let n=[],o=(e,o)=>n.push(...e,...o),t=[[-.85,-.85],[.85,-.85],[.85,.85],[-.85,.85]];for(let[n,r]of t)o([n,0,r],[n,e,r]);for(let n=0;n<=e-3.1;n+=3.1)for(let e=0;e<4;e+=1){let[r,a]=t[e],[l,i]=t[(e+1)%4];o([r,n,a],[l,n,i]),Math.floor(n/3.1)%2==0?o([r,n,a],[l,n+3.1,i]):o([l,n,i],[r,n+3.1,a])}let r=e+6;for(let[n,a]of t)o([n,e,a],[0,r,0]);o([-9,e,0],[26,e,0]),o([0,r,0],[26,e,0]),o([0,r,0],[-9,e,0]);let a=n=>r+n/26*(e-r);for(let n=3;n<23;n+=4)o([n,e,0],[n+4,a(n+4),0]);return o([-9,e-1.2,0],[-6.6,e-1.2,0]),o([-9,e-1.2,0],[-9,e,0]),o([-6.6,e-1.2,0],[-6.6,e,0]),o([16.12,e,0],[16.12,e-.55*e,0]),o([15.32,e-.55*e,0],[16.92,e-.55*e,0]),new Float32Array(n)}(e.height),3)),n}),[o]),d=(0,r.useMemo)(()=>new l.mrM({color:new l.Q1f(v.A.outline),transparent:!0,opacity:0,depthWrite:!1}),[]),c=(0,r.useRef)(0);return(0,i.D)((e,n)=>{d.opacity=(0,f.Ln)(f.Me.progress,f.A9.build[0],f.A9.build[0]+.06)*(1-(0,f.Ln)(f.Me.progress,f.A9.build[1]-.18,f.A9.build[1]))*.55,d.visible=d.opacity>.01,d.visible&&(c.current+=n,o.forEach((e,n)=>{let o=a.current[n];o&&(o.rotation.y=e.angle+c.current*e.slew)}))}),(0,t.jsx)(t.Fragment,{children:o.map((e,n)=>(0,t.jsx)("lineSegments",{ref:e=>{a.current[n]=e},geometry:u[n],material:d,position:[e.x,0,e.z],renderOrder:2,frustumCulled:!1},"".concat(e.x,"-").concat(e.z)))})}function x(e){let{ceiling:n,onDowngrade:o}=e,t=(0,i.C)(e=>e.gl),a=(0,r.useRef)(0),l=(0,r.useRef)(0),u=(0,r.useRef)(0);return(0,i.D)((e,r)=>{if(u.current<90){u.current+=1;return}if(l.current>=2||(a.current=1e3*r>24?a.current+1:Math.max(0,a.current-2),a.current<45))return;l.current+=1,a.current=0;let i=1===l.current?Math.min(n,1.2):1;t.setPixelRatio(i),null==o||o(l.current)}),null}function w(e){let{count:n,pixelRatio:o}=e,a=(0,r.useMemo)(()=>{let e=new l.LoY,o=new Float32Array(3*n),t=new Float32Array(n),r=new Float32Array(n),a=new Float32Array(n);for(let e=0;e<n;e+=1)o[3*e+0]=(Math.random()-.5)*170,o[3*e+1]=0,o[3*e+2]=(Math.random()-.5)*170,t[e]=2.5+6*Math.random(),r[e]=.8+2.4*Math.random(),a[e]=Math.random();return e.setAttribute("position",new l.THS(o,3)),e.setAttribute("aSpeed",new l.THS(t,1)),e.setAttribute("aScale",new l.THS(r,1)),e.setAttribute("aPhase",new l.THS(a,1)),e.boundingSphere=new l.iyt(new l.Pq0(0,55,0),400),e},[n]),u=(0,r.useMemo)(()=>new l.BKk({vertexShader:"\n  attribute float aSpeed;\n  attribute float aScale;\n  attribute float aPhase;\n\n  uniform float uTime;\n  uniform float uIntensity;\n  uniform float uHeight;\n  uniform float uPixelRatio;\n\n  varying float vAlpha;\n\n  void main() {\n    vec3 p = position;\n\n    // Падение по кольцу: долетев до земли, частица мгновенно возвращается наверх.\n    float fall = mod(uTime * aSpeed + aPhase * uHeight, uHeight);\n    p.y = uHeight - fall;\n\n    // Ветер сносит вбок и слегка закручивает.\n    p.x += sin(uTime * 0.35 + aPhase * 6.28) * 3.5 + fall * 0.32;\n    p.z += cos(uTime * 0.27 + aPhase * 6.28) * 2.4;\n\n    vec4 viewPos = modelViewMatrix * vec4(p, 1.0);\n    gl_Position = projectionMatrix * viewPos;\n\n    // Дальние снежинки мельче — иначе снег выглядит плоской пеленой.\n    gl_PointSize = aScale * uPixelRatio * (170.0 / max(-viewPos.z, 1.0));\n\n    // Гасим у самой земли и у верхней границы, чтобы не было видно стыка цикла.\n    float edge = smoothstep(0.0, 6.0, p.y) * (1.0 - smoothstep(uHeight - 12.0, uHeight, p.y));\n    vAlpha = edge * uIntensity;\n  }\n",fragmentShader:"\n  precision mediump float;\n  uniform vec3 uColor;\n  varying float vAlpha;\n\n  void main() {\n    // Круглая мягкая точка вместо квадрата.\n    vec2 d = gl_PointCoord - 0.5;\n    float mask = 1.0 - smoothstep(0.22, 0.5, length(d));\n    float alpha = mask * vAlpha;\n    if (alpha < 0.01) discard;\n    gl_FragColor = vec4(uColor, alpha);\n  }\n",transparent:!0,depthWrite:!1,blending:l.EZo,uniforms:{uTime:{value:0},uIntensity:{value:0},uHeight:{value:110},uPixelRatio:{value:o},uColor:{value:new l.Q1f(v.A.snow)}}}),[o]),s=(0,r.useRef)(null);return(0,i.D)((e,n)=>{let{camera:o}=e,t=s.current;t&&t.position.set(o.position.x,o.position.y-55,o.position.z),u.uniforms.uTime.value+=n;let r=.45*(0,f.Ln)(f.Me.progress,f.A9.lights[0],f.A9.lights[1]),a=.55*(0,f.Ln)(f.Me.progress,f.A9.pullback[0],f.A9.pullback[1]);u.uniforms.uIntensity.value=r+a}),(0,t.jsx)("points",{ref:s,geometry:a,material:u,renderOrder:3})}let y=[{at:0,pos:[0,1.7,64],look:[0,7,0],fov:62},{at:.22,pos:[1.5,1.1,46],look:[0,-3,6],fov:68},{at:.45,pos:[-9,24,74],look:[0,26,0],fov:58},{at:.7,pos:[-30,62,120],look:[0,30,-10],fov:52},{at:1,pos:[-66,130,224],look:[0,16,-24],fov:44}];function M(e,n,o){let t=0;for(;t<y.length-2&&e>y[t+1].at;)t+=1;let r=y[t],a=y[t+1],i=l.cj9.clamp((e-r.at)/(a.at-r.at),0,1),u=i*i*(3-2*i);return o.set(l.cj9.lerp(r[n][0],a[n][0],u),l.cj9.lerp(r[n][1],a[n][1],u),l.cj9.lerp(r[n][2],a[n][2],u)),l.cj9.lerp(r.fov,a.fov,u)}function b(e){let{parallax:n}=e,{camera:o}=(0,i.C)(),t=(0,r.useRef)(new l.Pq0),a=(0,r.useRef)(new l.Pq0);return(0,i.D)((e,r)=>{let l=1-Math.pow(.0015,r);f.Me.eased+=(f.Me.progress-f.Me.eased)*l;let i=f.Me.eased,u=M(i,"pos",t.current);M(i,"look",a.current),n&&(t.current.x+=3.2*f.Me.pointerX,t.current.y+=1.8*f.Me.pointerY),o.position.copy(t.current),o.lookAt(a.current),Math.abs(o.fov-u)>.01&&(o.fov=u,o.updateProjectionMatrix())}),null}var A=o(5985);function S(){let{isMobile:e,isLowPower:n,reducedMotion:o,ready:i}=(0,A.Y)(),u=!i||e||n,s=u?1.35:1.6,[v,y]=(0,r.useState)(!0),M=(0,r.useRef)(null);(0,r.useEffect)(()=>{let e=M.current;if(!e||"undefined"==typeof IntersectionObserver)return;let n=new IntersectionObserver(e=>{let[n]=e;return y(n.isIntersecting)},{rootMargin:"10% 0px"});return n.observe(e),()=>n.disconnect()},[]);let[S,D]=(0,r.useState)(0),F=(0,r.useCallback)(e=>D(e),[]),C=u?90:190,j=S>=2?Math.round(.45*C):C;return(0,r.useEffect)(()=>{if(e||o)return;let n=e=>{f.Me.pointerX=e.clientX/window.innerWidth*2-1,f.Me.pointerY=-(e.clientY/window.innerHeight*2-1)};return window.addEventListener("pointermove",n,{passive:!0}),()=>window.removeEventListener("pointermove",n)},[e,o]),(0,t.jsx)("div",{ref:M,className:"absolute inset-0",children:(0,t.jsxs)(a.Hl,{dpr:[1,s],gl:{antialias:!1,powerPreference:"high-performance",alpha:!1},camera:{fov:62,near:.1,far:700,position:[0,1.7,64]},onCreated:e=>{let{gl:n,scene:o}=e;n.setClearColor(new l.Q1f("#050607"),1),o.fog=new l.jUj("#050607",90,320)},frameloop:v?"always":"never",children:[(0,t.jsx)(x,{ceiling:s,onDowngrade:F}),(0,t.jsx)(b,{parallax:!e&&!o}),(0,t.jsx)(h,{}),(0,t.jsx)(m,{}),(0,t.jsx)(c,{count:j}),(0,t.jsx)(p,{}),(0,t.jsx)(d,{count:j}),!u&&0===S&&(0,t.jsx)(g,{count:j}),(0,t.jsx)(w,{count:u?900:3200,pixelRatio:s})]})})}}}]);