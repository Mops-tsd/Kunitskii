/**
 * Палитра сайта — единственное место, где живут цвета.
 *
 * Тема холодная и намеренно одноцветная: акцент один, различается только
 * яркостью. Это держит 3D-сцену цельной — если развести элементы по разным
 * тонам, город рассыпается на несвязанные детали.
 *
 * Иерархия по яркости, от тусклого к самому светлому:
 *   сетка чертежа → контур зданий → сваи → окна
 *
 * Те же значения продублированы в tailwind.config.ts и globals.css
 * для CSS-части. При смене акцента править нужно все три места.
 */

export const PALETTE = {
  /** Фон сцены и страницы. */
  void: '#050607',
  /** Бетон зданий — чуть светлее фона, чтобы объём читался. */
  body: '#151C22',

  /** Основной акцент: арктический лёд. Для мелких меток и линий. */
  signal: '#5FD4E8',
  /**
   * Приглушённый вариант акцента для крупной типографики.
   *
   * На мелком тексте насыщенный лёд читается как точность, но фамилия
   * во весь экран тем же цветом выглядит неоном — а сайту нужна
   * солидность. Поэтому у крупного кегля свой, менее звонкий тон.
   */
  signalMuted: '#7FB6C6',

  // --- Сцена, по возрастанию яркости
  /** Мелкая сетка чертежа — почти на грани видимости. */
  gridMinor: '#1E4E5C',
  /** Крупная сетка — опорные линии разметки. */
  gridMajor: '#2F8FA6',
  /** Контур зданий: заметнее сетки, тусклее окон. */
  outline: '#4FB8D0',
  /** Сваи светятся ярче всего — на них строится первая фаза анимации. */
  pile: '#6FE0F2',
  /** Свет в окнах — почти белый, с холодным подтоном. */
  window: '#CFEFFA',
  /** Снег. */
  snow: '#DCEEF5',
} as const;
